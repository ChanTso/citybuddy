# 单热点160/s重复成单工作点

CityBuddy commit：`76c293178923bf78e747ab1ba9590e6348108ad8`。本机Apple M4，Docker配置8CPU/14GB（实际可见14,638,391,296 bytes），Commerce4CPU，k6与服务同Docker网络。单活动、单SKU、48050买家/库存/配额；160次新请求/秒持续300秒，无正向业务预热。与a160r2同源码、JAR、配置及分段原生采样；数据库历史继续累积，不宣称完全相同内存初态。

- 48001次全部fresh ADMITTED，HTTP失败、丢弃、中断、负HTTP时长均0。HTTP p50约2.43ms、p99约34.06ms，与成单等待分开。
- SQL48001笔全部UNPAID/SENT；缺单、重复、绑定、成单账本与deadline错误均0。库存49+48001=初始48050，库存/配额扣减流水各48001。
- 预约落库至成单等待p50 121.342ms、p95 6185.285ms、p99 10194.936ms、最大12254.555ms，无负值。保留启动阶段。
- SQL五个完整分钟产出分别147.85、170.2667、160.0833、159.9000、159.8500单/s；另有末尾不足一分钟124笔，不把124/60当末一分钟能力。预约分钟分组p99分别11553.349、11090.424、336.065、467.829、300.914ms。首段短暂排队后追上输入，晚段没有持续积压。
- 5秒采样最大未成单773笔、最大待派发16笔。最后HTTP于14:21:17.667777 UTC完成；首个确认48001成单且全部SENT的SQL采样结束观测上界约4.59秒，前个未清空采样在停压前，因此只能说停压后首个采样确认清空，不能写精确清空耗时。
- 最后订单14:21:17.772123 UTC。正式after与原生drained已保存。随后事务消息Diff/Inflight均0、全局Redis handoff ZCARD=0，见`mq_a160r3_76c2931_20260907T141442Z_ordered_1788791342148786000.txt`。

资源原件62个样本中，Commerce CPU点采样最高337.84%；cgroup节流计数增加2次、累计126277微秒。它不支持把本点称为Commerce持续打满，也不能凭单个峰值定位其他依赖上限。

本点与a160r2均在最后三个完整分钟跟随160/s输入，形成重复工作点，不是容量上限。全窗成单p99分别约8.66秒和10.19秒，不能用后段约几百毫秒替换。下一点按登记200/s持续300秒，观察是否持续积压及停压恢复，不自动二分最高吞吐。

原始文件前缀：`ladder_a160r3_76c2931_20260907T141442Z`、`k6_a160r3_*`、`orders_a160r3_*`、`native_a160r3_*`和上述MQ文件。native before/120/240/input_end/drained保存原始performance_schema与状态，不仅凭累计after归因。首次人工MQ查询漏topic且Redis容器名错误，失败原件保留，之后按真实topic/容器重查；这是观察命令错误，不是服务故障。

观察进程在输入父进程退出时结束；最后一个原始采样已确认全部清空，因此成单结果完整。未将后续无人采样时间冒称持续观察。自然未付取消与库存恢复另存，不纳入正式成单性能。

## 正式输入区间的原生统计

| 区间 | 实际秒数 | COMMIT次数/平均ms | buffer pool物理读增量 | wait_free增量 | 行锁等待累计增量ms |
|---|---:|---:|---:|---:|---:|
| before→during120 | 120.107 | 36450/1.131 | 219977 | 157 | 24992 |
| during120→during240 | 120.012 | 38428/1.017 | 360006 | 258 | 23749 |
| during240→input_end | 71.107 | 21176/1.053 | 274215 | 187 | 14461 |

MySQL保持128MiB buffer pool、100MiB redo、flush-at-commit=1及sync-binlog=1。各段日志等待增量0。存在物理读和buffer pool等待，但不能从这些累计增量独立宣布唯一瓶颈；本点晚段仍跟随输入。

## 未付取消收尾（不混入成单性能）

14:57:45 UTC进度SQL首次确认48001笔全部CANCELLED；最晚unpaid_deadline为14:36:17.772050。14:58:21完整SQL确认库存48050、成单/取消流水各48001、坏绑定/未结束预约/未派发超时均0。MQ两topic Diff/Inflight及全局Redis handoff均0。原件为`recovery_a160r3_76c2931_20260907T141442Z_final_1788793101496609000.txt`与`recovery_mq_a160r3_76c2931_20260907T141442Z_1788793130616764000.txt`。

大批未付款的取消明显慢于成单。本点自然收尾中，14:35:25原生累计快照记录取消使用的活动占额SUM共6444次、平均31.654ms；源码每取消一笔重新汇总当前占额。它是实际后续优化候选，但本轮不增加取消峰值实验或配额计数器重构。五分钟成单结果不代表覆盖大批未付取消的全生命周期稳态容量。

[原始输出无损归档](seckill-orders-a160r3-20260907.tar.gz)。包含该组原始k6、SQL及资源记录，未用重新计算的结果替代原件。
