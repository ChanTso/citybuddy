# 单热点200/s：准入完整，但成单持续积压

CityBuddy commit：`76c293178923bf78e747ab1ba9590e6348108ad8`。M4，Docker配置8CPU/14GB（实际可见14,638,391,296 bytes），Commerce4CPU；k6与服务共用Docker网络。单活动/单SKU，60050独立买家、库存和配额，200/s持续300秒，没有正向业务预热。与两个160点同源码/JAR/业务配置和分段观察；依赖本次保留运行、历史数据累积，不宣称完全相同缓存初态。

- 60001次全部fresh ADMITTED；HTTP失败、丢弃、中断、负HTTP时长均0。HTTP p50约2.47ms、p99约37.92ms。
- SQL60001笔全部正确成单，观察器确认UNPAID/SENT；缺单、重复用户/预约、绑定与成单账本错误、意外账本、超过付款期限才建单均0。库存49+60001=初始60050；成单库存/配额扣减各60001。
- SQL预约落库至成单等待p50 **36989.468ms**、p95 **87338.829ms**、p99 **91124.602ms**、最大95255.726ms，无负值。
- 五个完整输入分钟的订单产出分别152.70、161.40、160.6167、159.8833、157.55单/s；后段持续低于实际约200/s获准输入。预约分钟p99依次24272.219、37086.750、53186.645、73565.021、93995.008ms，等待不断增长。5秒采样最大未成单12294笔、最大待派发16笔。
- 最后HTTP结束15:06:28.706890 UTC，最后订单15:07:58.323691；按最后未清空采样与首次全部ORDERED/SENT采样，停压恢复观测区间约 **(89.16,95.04]秒**。不称精确95.04秒；原始采样时间与查询耗时保留。
- 停压后有一个完整60秒窗口产出139.4667单/s；最后4104笔属于不足一分钟的尾部，不能把4104/60当末一分钟能力。
- 事务消息Diff/Inflight及全局Redis handoff均0，原件`mq_a200r1_76c2931_20260907T145922Z_ordered_1788793767858377000.txt`。未来未付取消另作生命周期收尾，不混入本成单结论。

结论：本版本200/s是持续成单过载观察点，尽管准入全部成功、最终SQL正确。结合两个160/s重复点，当前可以报告“单热点160/s持续5分钟可重复完成；200/s持续积压并在停压后恢复”。不做整数二分，不将批均154.09单/s称精确容量，也不将旧版本或不同观察方式拼成优化收益。

原始资源样本中Commerce CPU最高261.08%，cgroup节流增量4次/66713微秒，没有显示Commerce持续打满4核。源码的成单消费者串行处理消息并逐笔事务/ACK；本点说明这条实际成单链路产出不足，尚不能仅凭CPU余量把某一条SQL或MQ认定唯一限制。已有节拍优化足以构成因果故事，不为本轮定稿追加批量事务或并发消费重构。

## 观察查询与原件

首次完整after SQL触及原观察脚本15秒客户端超时，`orders_a200r1_76c2931_20260907T145922Z_after.txt`保留SQL及失败说明，不能拿它当核对通过。正式输入和采样已结束后，使用同一份已提交`bench/sql/seckill_orders.sql`、独立结果文件，将只读客户端上限设为60秒，完整执行成功。权威结果在`orders_a200r1_76c2931_20260907T145922Z_after_retry60.txt`，只改变停压后观察等待上限，没有改负载、业务或原失败记录。

k6原件见同label的console、summary、points及ladder steps；进度/资源见orders progress/resources，原生before/during120/during240/input_end/drained快照另存。所有实测原件保留完整SHA。自然取消及最终库存恢复将在后续独立记录中补齐。

## 积压曲线

![160与200输入下的订单积压](seckill_order_backlog_20260907.png)

左图为两个160/s重复点，右图为200/s点，纵轴尺度不同。每组以自身观察器启动为monotonic时间零点，约5秒一次的SQL采样原样连线；虚线按原始HTTP时间映射输入结束的大致位置，不宣称不同点毫秒级对齐。曲线用于区分短时积压回落与持续失衡，不替代各点完整SQL和延迟分位。

## 自然取消收尾（与成单测量分开）

15:59:22 UTC首次进度确认60001笔全部取消；最晚付款期限15:22:58.323578。16:00:08完整SQL确认库存恢复60050，成单与取消流水各60001，未结预约、错误绑定、未完成准入及未派发超时均0。两消息组Diff/Inflight及Redis handoff均0。原件：`recovery_a200r1_76c2931_20260907T145922Z_final_1788796808588647000.txt`、`recovery_mq_a200r1_76c2931_20260907T145922Z_1788796833782680000.txt`。大批未付订单的取消存在明显滞后，不能将本报告的5分钟成单工作点表述为整个订单生命周期的稳态容量。

[原始输出无损归档](seckill-orders-a200-20260907.tar.gz)。包含该组原始k6、SQL及资源记录，未用重新计算的结果替代原件。
