# CB-150 — Agent metrics and optional no-op trace sink

## Referenced contracts

- [Agent, tool, action, and evidence boundaries](../CONTRACTS.md#contract-agent-action-evidence)
- [Retrieval and knowledge boundaries](../CONTRACTS.md#contract-retrieval-knowledge)
- [Evaluation-only CityBuddy boundary](../CONTRACTS.md#contract-evaluation-boundary)
- [Truth hierarchy](../CONTRACTS.md#contract-truth-hierarchy)
- [Risks and change control](../CONTRACTS.md#contracts-risks-change-control)

## Goal

Add the smallest low-cardinality, purely observational metrics for current verified Agent paths and
the FAQ/backend counts required by CB-152, together with an optional Agent trace sink that defaults
to `Noop`. Do not instrument `auth-service` or `commerce-service`.

## In scope

- Define count and duration observations for the Agent operations `knowledge.search`, chat turn,
  PendingAction prepare, clarification, decline, and expiry. Each operation has a bounded outcome
  classification; identifiers, content, and raw dependency/model errors cannot become labels.
- Define separate mapping-cache and answer-cache counters with the bounded terminal outcomes `hit`,
  `miss`, `bypass`, `unavailable`, and `invalid`. Each eligible logical lookup contributes exactly
  one terminal outcome at each cache layer it reaches; replay and internal retry do not add another
  logical lookup outcome. Calculate FAQ hit rate separately for mapping and answer as
  `hit / (hit + miss)`; bypass, unavailable, and invalid are reported but excluded from that
  denominator.
- Add `citybuddy_knowledge_backend_decisions_total` with the single bounded label `decision` and
  exact values `cache_served` and `elasticsearch_issued`. Each non-replayed `knowledge.search`
  logical execution that reaches and completes the FAQ-cache/Elasticsearch choice records at most
  one decision: a valid cache hit with no Elasticsearch call records `cache_served`; a cache miss,
  bypass, unavailable result, or invalid result records `elasticsearch_issued` only when it actually
  invokes the logical Elasticsearch knowledge search. Calculate Elasticsearch knowledge-search
  avoidance as `cache_served / (cache_served + elasticsearch_issued)`.
- Exclude replay, rejection before the backend choice, malformed tool calls, budget or circuit
  rejection before the choice, and paths with neither a usable cache result nor an actual
  Elasticsearch call from the backend-decision denominator. Their operation outcomes remain
  observable separately.
- Record each actual provider attempt by the bounded role `primary`, `fallback`, or `reranker` and a
  bounded outcome. Attempt counters describe actual outbound attempts only, remain completely
  separate from the FAQ and Elasticsearch-avoidance denominators, and are runtime diagnostics rather
  than a model-call-saving result. Fallback and retry each record their actual attempts; replay or
  cache facts cannot fabricate an attempt.
- Provide an Agent-internal Prometheus text endpoint backed by a custom `CollectorRegistry`. It is
  disabled by default, is absent or returns `404` while disabled, is excluded from public OpenAPI,
  and registers no default process or garbage-collection metrics.
- Provide an Agent-only optional trace sink with a bounded queue, bounded export timeout, zero retry,
  and default `Noop`. Trace enqueue/export failure, timeout, malformed response, and shutdown cannot
  determine business or evaluation success.
- Maintain a machine-readable metric inventory and executable cardinality tests that close the set
  of metric names, units, types, labels, bounded values, and verified Agent producers.
- Exclude user, token, session, turn, trace, order, product, reservation, payment, refund,
  PendingAction arguments, prompt/reply/retrieved content, server-only reasons, credentials, and raw
  errors from all metric labels and trace payloads.

## Acceptance criteria

1. The machine-readable Agent metric inventory is closed and stable: every metric has one name,
   unit, type, bounded label set, bounded unknown/outcome handling, and documented verified producer;
   executable cardinality tests reject additions outside it.
2. Operation count/duration observations cover only `knowledge.search`, chat turn, and PendingAction
   prepare/clarification/decline/expiry, without changing their response, authorization, transaction,
   state, retry, or durable evidence behavior.
3. Mapping and answer FAQ cache hit rates are separately executable as `hit / (hit + miss)`.
   Replay and internal retry do not double-count a logical lookup, and bypass, unavailable, and
   invalid remain separately reported rather than masquerading as `miss`.
4. Elasticsearch knowledge-search avoidance is executable as
   `cache_served / (cache_served + elasticsearch_issued)`. Each eligible backend choice records
   exactly one decision; a cache hit cannot also record `elasticsearch_issued`, a miss without an
   actual logical Elasticsearch knowledge-search invocation cannot fabricate
   `elasticsearch_issued`, replay records no new decision, and a path rejected or unable to complete
   a choice before an actual invocation is excluded.
5. Provider-attempt counters represent actual primary, fallback, and reranker outbound attempts with
   bounded outcomes and remain diagnostic only. They are separate from both cache formulas, do not
   support a model-saving claim, and cannot be inferred from replay or cache outcomes.
6. Metrics are disabled by default. The internal Prometheus endpoint is absent or `404` when
   disabled, is never in public OpenAPI, uses only the custom registry, and exposes no default
   process or garbage-collection collectors when enabled.
7. Tracing defaults to `Noop`; the enabled sink has a bounded queue and timeout and performs zero
   retry. Missing configuration, outage, timeout, malformed export, queue saturation, and shutdown
   leave business responses, state, and evidence unchanged.
8. Metrics and traces contain none of the prohibited identity, business, content, credential, or
   server-only fields. No assertion, evaluation result, retry decision, state transition, or public
   response reads from either observational channel.
9. Metrics and tracing remain purely observational and leave the CB-112 FAQ cache lookup,
   Elasticsearch fallback, reranker/sufficiency, citation, and durable-evidence behavior unchanged.
10. No Auth or Commerce instrumentation, Agent confirmation/receipt/turn commit, memory, handoff,
   measurement execution, dashboard, or cross-service trace behavior is added.

## Rejection paths

- Reject dynamic identifiers, free text, secrets/private content, unbounded names or values, and raw
  dependency/model errors in metrics or traces.
- Reject an `avoided` counter without a real production branch, calling Elasticsearch avoidance
  model-call saving, or deriving uncontrolled saving from provider-attempt differences.
- Reject counting replay, denial, malformed requests, pre-choice budget/circuit rejection, or a
  failed-to-complete backend choice as cache/model saving; reject cache-hit and
  `elasticsearch_issued` double counting or issued counts without an actual logical Elasticsearch
  knowledge-search invocation.
- Reject changing Agent decisions, adding a pre-model/direct-cache shortcut, bypassing the existing
  reranker/sufficiency/evidence path, or skipping the Agent loop to manufacture a metric result.
- Reject ambiguous FAQ/backend formulas, replay/retry/fallback double counting, and any result that
  cannot be reconstructed from the closed Agent producer events.
- Reject tracing as a required dependency, trace success as evidence, retry-on-export amplification,
  unbounded queue/timeout behavior, or export failure changing business results.
- Reject introducing Java instrumentation in the name of unified three-service observability,
  including an Auth or Commerce registry, metrics dependency, scrape endpoint, operation producer,
  Micrometer integration, or trace implementation.
- Reject adding Auth or Commerce metrics during CB-152, treating their absence as invalidating a
  performance run, or guessing a first bottleneck when the permitted evidence cannot isolate one;
  the required result in that case is `indeterminate`.
- Reject counters or traces for unimplemented Agent confirmation, receipt Turn commit, memory,
  handoff, or failure-candidate export.

## Out of scope

- `auth-service` metrics, `commerce-service` metrics, Micrometer, Java metrics endpoints, and
  cross-service distributed tracing;
- Prometheus server, Grafana, dashboards, alert routing, SLO policy, production observability
  infrastructure, real-provider telemetry, log aggregation, or deployment;
- Agent successful confirmation/receipt projection/turn commit, memory/PII, handoff, candidate
  export, cart/storefront expansion, demo reset/drills, and load or quality execution.

## Required evidence

- Agent metric inventory and cardinality tests covering names, units, types, labels, bounded values,
  and exact producer coverage;
- mapping/answer FAQ outcome matrices covering hit, miss, bypass, unavailable, invalid, replay,
  internal retry, and dependency failure, with `hit / (hit + miss)` reconstructed separately for
  each layer;
- an exact `cache_served`/`elasticsearch_issued` backend-decision matrix proving cache-hit zero
  Elasticsearch calls; real logical Elasticsearch knowledge-search invocations after miss, bypass,
  unavailable, or invalid cache handling; zero new replay decisions; pre-choice exclusion; and one
  decision per eligible choice;
- provider-attempt diagnostic matrices covering actual primary, fallback, reranker, retry, denial,
  and bounded dependency outcomes without a model-saving claim;
- Agent operation count/duration evidence for `knowledge.search`, chat turn, and PendingAction
  prepare/clarification/decline/expiry;
- disabled/absent and enabled Agent scrape-endpoint tests, including public-OpenAPI exclusion, custom
  registry isolation, and absence of default process/garbage-collection collectors;
- default-`Noop`, enabled fake sink, queue saturation, timeout, outage, malformed-export, shutdown,
  and zero-retry trace tests;
- privacy, redaction, secret, prohibited-field, business-equivalence, and no-truth-reader evidence;
- business-equivalence plus real retrieval and evaluation integration evidence proving metrics and
  tracing do not alter CB-112 retrieval, rerank, sufficiency, citation, or durable-evidence behavior;
- complete-diff self-review, fresh independent review, and final repository checks.

## Completion record

| Field | Value |
|---|---|
| Branch | `codex/cb-150-agent-metrics-noop-trace` |
| PR | [#74](https://github.com/ChanTso/citybuddy/pull/74) |
| Commits | `b9f8fe9938ed519a3694ec79a5c9068006d7d448` (bounded Agent metrics and optional trace sink); `71c2c84505840db531cc3d776da0c52986d9efca` (complete observability evidence matrices); `bc3c08542445483585a17d47402ec9c576fdf536` (retrieval metric probe validation); `c723f39875cfaf635acdacb2cc10ea263a62bc6a` (opaque evaluation-session binding); `82eed58feb04d8a22407b771bb33063477e634ab` (response-loss replay oracle); factual closeout in this commit |
| Tests | Focused Agent metrics, application, action-control, and script-contract tests passed 19 tests after the final oracle correction; the fresh evaluation-sandbox integration passed the complete inherited business lifecycle and all six exact CB-150 scrape assertions with the privacy rejection guard; the complete local `make ci` passed. The exact implementation head received an independent read-only `NO BLOCKER`, with 217 changed tests, shell syntax, complete-diff whitespace, clean-tree, and local/remote-head checks independently passing. Final closeout-head review and GitHub required checks are recorded in PR #74. |
| Notes | Agent-only custom-registry metrics close names, labels, bounded values, producers, mapping/answer hit-rate denominators, backend-decision avoidance, provider attempts, and verified operation outcomes without adding Auth or Commerce instrumentation. Metrics are disabled by default and expose no default collectors or public OpenAPI route. The optional bounded, timeout-limited, zero-retry, identifier-free trace mirror defaults to `Noop` and never determines business or evidence truth. The evaluation response-loss path records the single stable `pending_action_prepare/replay` outcome after Commerce commits and bounded Agent replay returns `replayed=true`; it does not fabricate a simultaneous success. All opaque session CLI values are passed as bound option data, including legal leading-dash values. No CB-151 behavior is implemented. |
